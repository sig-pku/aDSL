# adsl-core

Runtime-independent primitive modeling for aDSL object and scene programs.

The package provides primitive and boolean geometry, transforms, bounds,
relational layout, articulated asset trees, and URDF/GLB/manifest export.

```cmd
python -m pip install -e .
```

Optional mesh and Blender export dependencies are available through the
`mesh` and `render` extras.
